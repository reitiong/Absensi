import React, { Component } from "react";
import { Text, Container, View } from "native-base";
import HomeDos from "../HomeDos";
import HomeMhs from "../HomeMhs";

export default class Home extends Component {
  render() {
    return (
      <View>
        <Text> textInComponent </Text>
      </View>
    );
  }
}
